import {
    StyleSheet
} from 'react-native';

const header = StyleSheet.create({
    Head: {
        width: 660,
        height: 30,
        marginTop: 8,
        flexDirection: 'row',
        justifyContent: 'flex-start',
        alignItems: 'center',
    },
});

module.exports = header;